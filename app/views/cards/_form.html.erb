<%= simple_form_for(@card) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :brand, collection: options_for_enum(Card, :brand, @card) %>
    <%= f.input :title %>
    <%= f.input :credit_limit %>
    <%= f.input :billing_day %>
    <%= f.input :payment_day %>
    <%= f.association :account %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
