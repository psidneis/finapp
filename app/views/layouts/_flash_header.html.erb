<% flash.each do |name, msg| %>
  <% if msg.present? %>
    <div class="alert alert-<%= name == 'notice' ? "success" : (name == 'warning' ? "warning" : "error") %>">
      <a class="close" data-dismiss="alert">×</a>
      <%= msg.html_safe %>
    </div>
  <% end %>
<% end %>